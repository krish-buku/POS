// TODO: remove callers of StatusBadge; use <StatusPill /> directly.
// This file is a thin compatibility re-export so the migration can land incrementally.
export { StatusPill as StatusBadge } from '../ui/StatusPill';
