server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:5173"
              - "http://localhost:8081"
            allowedMethods:
              - GET
              - POST
              - PUT
              - PATCH
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: true
            maxAge: 3600
      routes:
        - id: auth-service
          uri: lb://auth-service
          predicates:
            - Path=/api/auth/**

        - id: user-business-service
          uri: lb://user-business-service
          predicates:
            - Path=/api/businesses/**

        - id: staff-service
          uri: lb://staff-service
          predicates:
            - Path=/api/staff/**

        - id: menu-service
          uri: lb://menu-service
          predicates:
            - Path=/api/menu/**

        - id: table-service
          uri: lb://table-service
          predicates:
            - Path=/api/tables/**

        - id: order-service
          uri: lb://order-service
          predicates:
            - Path=/api/orders/**

        - id: payment-service
          uri: lb://payment-service
          predicates:
            - Path=/api/payments/**

        - id: kitchen-service
          uri: lb://kitchen-service
          predicates:
            - Path=/api/kitchen/**

        - id: notification-service
          uri: lb://notification-service
          predicates:
            - Path=/api/notifications/**

        - id: ai-image-service
          uri: lb://ai-image-service
          predicates:
            - Path=/api/images/**

        - id: report-service
          uri: lb://report-service
          predicates:
            - Path=/api/reports/**

        - id: receipt-service
          uri: lb://receipt-service
          predicates:
            - Path=/api/receipts/**

        - id: file-storage-service
          uri: lb://file-storage-service
          predicates:
            - Path=/api/files/**

        - id: realtime-gateway
          uri: lb://realtime-gateway
          predicates:
            - Path=/api/realtime/**,/ws/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
