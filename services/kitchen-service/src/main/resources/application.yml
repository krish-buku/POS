server:
  port: 8088
spring:
  application:
    name: kitchen-service
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
springdoc:
  api-docs:
    path: /api/kitchen/v3/api-docs
management:
  endpoints:
    web:
      exposure:
        include: health,info
