server:
  port: 8084

spring:
  application:
    name: menu-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

springdoc:
  api-docs:
    path: /api/menu/v3/api-docs
  swagger-ui:
    path: /api/menu/swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info
