server:
  port: 8094
spring:
  application:
    name: realtime-gateway
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
springdoc:
  api-docs:
    path: /api/realtime/v3/api-docs
management:
  endpoints:
    web:
      exposure:
        include: health,info
