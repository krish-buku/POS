server:
  port: 8092
spring:
  application:
    name: receipt-service
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true
springdoc:
  api-docs:
    path: /api/receipts/v3/api-docs
management:
  endpoints:
    web:
      exposure:
        include: health,info
