server:
  port: 8083

spring:
  application:
    name: staff-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

springdoc:
  api-docs:
    path: /api/staff/v3/api-docs
  swagger-ui:
    path: /api/staff/swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info
