server:
  port: 8082

spring:
  application:
    name: user-business-service

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

springdoc:
  api-docs:
    path: /api/businesses/v3/api-docs
  swagger-ui:
    path: /api/businesses/swagger-ui.html

management:
  endpoints:
    web:
      exposure:
        include: health,info
